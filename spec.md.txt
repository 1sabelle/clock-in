# Auto Time Registration — Build Spec

## Overview

Build a Windows startup app that shows a toast notification on login with a **"Clock in"** button. When clicked, Playwright runs headlessly to log in and register "In" time. The toast closes automatically when done. A **"Dismiss"** button allows skipping registration.

---

## Project Structure

```
C:\ClockIn\
├── store_credentials.py   # Run once to save credentials, then delete
├── clockin.py             # Main script
└── dist\
    └── clockin.exe        # Packaged output from PyInstaller
```

---

## Step 1 — Store Credentials (run once, then delete)

Create `store_credentials.py`:

```python
import keyring
import getpass
import os
from dotenv import load_dotenv

load_dotenv()
KEYRING_SERVICE = os.getenv("KEYRING_SERVICE")

keyring.set_password(KEYRING_SERVICE, "username", input("Email: "))
keyring.set_password(KEYRING_SERVICE, "password", getpass.getpass("Password: "))

print("Credentials saved securely!")
```

Run it once:
```
python store_credentials.py
```

Credentials are entered interactively and never written to disk. Delete the file when done.

---

## Step 2 — Main Script

Create `clockin.py`:

- On startup, show a toast notification in the lower right corner with a **"Clock in"** button and a **"Dismiss"** button
- Clicking **"Clock in"** disables both buttons, updates the label to "Clocking in...", and runs Playwright in a background thread
- When Playwright finishes, the toast closes automatically
- Clicking **"Dismiss"** closes the toast without registering

All configuration (URL, element IDs, keyring service name) is loaded from `.env` which is not checked in.

---

## Step 3 — Test the script

```
cd C:\ClockIn
python clockin.py
```

Expected behaviour:
- A toast appears in the lower right corner with "Ready to clock in?" and two buttons
- Clicking "Clock in" triggers Playwright silently in the background
- Toast closes automatically when Playwright finishes
- Clicking "Dismiss" closes the toast without registering

---

## Step 4 — Package as .exe

```
cd C:\ClockIn
pyinstaller --onefile --noconsole clockin.py
```

Output: `C:\ClockIn\dist\clockin.exe`

---

## Step 5 — Register for Windows startup

### Option A — Startup Folder (easiest)
1. Press **Win + R**, type `shell:startup`, press Enter
2. Paste a shortcut to `C:\ClockIn\dist\clockin.exe` into that folder

### Option B — Registry (cleaner)
1. Press **Win + R**, type `regedit`, press Enter
2. Navigate to:
   ```
   HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run
   ```
3. Right-click right panel → **New → String Value**
4. Name: `TimeRegistration`
5. Value: `C:\ClockIn\dist\clockin.exe`
6. Click OK, close regedit

---

## Dependencies

Install before running:

```
pip install pyinstaller playwright keyring python-dotenv
playwright install chromium
```

---

## Configuration (.env)

All sensitive configuration lives in a local `.env` file (not checked in). Required keys:

| Key | Description |
|---|---|
| `URL` | Login page URL |
| `KEYRING_SERVICE` | Keyring service name for stored credentials |
| `EMAIL_FIELD` | CSS selector for email input |
| `PASSWORD_FIELD` | CSS selector for password input |
| `LOGIN_BUTTON` | CSS selector for login button |
| `TYPE_INPUT` | CSS selector for registration type dropdown |
| `REGISTER_BUTTON` | CSS selector for register button |

---

## Behaviour on startup

| Scenario | Result |
|---|---|
| User clicks "Clock in" | Toast updates to "Clocking in...", closes when done |
| User clicks "Dismiss" | Toast closes, nothing registered |
